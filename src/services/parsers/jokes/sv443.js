module.exports = {
    "random": (body) => JSON.parse(body),
    "categories": (body) => JSON.parse(body),
    "randomByCategory": (body) => JSON.parse(body),
    "search": (body) => JSON.parse(body)
};